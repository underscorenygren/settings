# Settings

This is a quick-setup repository and storage of Erik's default settings. 

## Installation

Navigate to your home directory and execute setup.sh:

```
cd ~
sh path/to/this/repo/setup.sh
```

## Uninstall

Unlinks the files installed by setup.sh

```
sh path/to/this/repo/uninstall.sh
```
